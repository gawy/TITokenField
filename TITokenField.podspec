Pod::Spec.new do |s|

  s.name         = 'TITokenField'
  s.version      = '0.1'
  s.summary      = ''

  s.homepage     = 'https://github.com/thermogl/TITokenField'
  s.license      = 'BSD'

  s.author       = { 'Tom Irving' => 'http://thermoglobalnuclearwar.com/' }

  s.source       = { :git => 'https://github.com/gawy/TITokenField.git' }

  s.platform     = :ios, '5.0'

  s.source_files = 'TITokenField.h', 'TITokenField.m'
  s.requires_arc = true


end

 
